import CreateFlatMainView from "@/mainViews/dashboard/createFlatMainView/CreateFlatMainView";

export const metadata = {
  title: "Add Flat Unit - Real Nest",
  description: "Add a new flat unit to property inventory",
};

export default function CreateFlatPage() {
  return <CreateFlatMainView />;
}
