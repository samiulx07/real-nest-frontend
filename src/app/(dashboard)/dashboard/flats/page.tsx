import FlatsMainView from "@/mainViews/dashboard/flatsMainView/FlatsMainView";

export const metadata = {
  title: "Flats & Units - Real Nest",
  description: "Browse and manage flat units inventory across all properties",
};

export default function FlatsPage() {
  return <FlatsMainView />;
}
