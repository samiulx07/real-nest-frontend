import CreatePropertyMainView from "@/mainViews/dashboard/createPropertyMainView/CreatePropertyMainView";

export const metadata = {
  title: "Add Property - Real Nest",
  description: "Create new property / building project listing",
};

export default function CreatePropertyPage() {
  return <CreatePropertyMainView />;
}
