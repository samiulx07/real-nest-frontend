import FlatsMainView from "@/mainViews/public/flatsMainView/FlatsMainView";

export const metadata = {
  title: "Available Flats - Real Nest",
  description: "Browse available flat units across residential building developments",
};

export default function FlatsPage() {
  return <FlatsMainView />;
}
